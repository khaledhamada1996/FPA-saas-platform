"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";

type Company = { id: string; name: string; base_currency: string; role: string; role_key: string };
const roles: Record<string, string> = { company_admin: "مدير النظام", admin: "مدير النظام", owner: "المالك", ceo: "الرئيس التنفيذي", cfo: "المدير المالي", finance_manager: "مدير مالي", fpa_analyst: "محلل FP&A", accountant: "محاسب", viewer: "مطلع", planner: "محلل FP&A" };

export default function WorkspacePage() {
  const router = useRouter(); const [company, setCompany] = useState<Company | null>(null); const [companies, setCompanies] = useState<Company[]>([]); const [open, setOpen] = useState(false); const [selectedIds, setSelectedIds] = useState<string[]>([]); const [loading, setLoading] = useState(true);
  useEffect(() => { const supabase = getSupabaseBrowserClient(); let alive = true; async function load() { const { data: user } = await supabase.auth.getUser(); if (!user.user) { router.replace("/login?next=/workspace"); return; } const { data, error } = await supabase.rpc("get_my_workspaces"); if (error || !data?.length) { router.replace("/start"); return; } const list = data as Company[]; const activeId = window.sessionStorage.getItem("activeOrganizationId"); const active = list.find((x) => x.id === activeId) ?? list[0]; let ids = [active.id]; try { const parsed = JSON.parse(window.sessionStorage.getItem("selectedOrganizationIds") || "[]"); if (Array.isArray(parsed) && parsed.length) ids = parsed.filter((id): id is string => typeof id === "string" && list.some((x) => x.id === id)); } catch {} if (alive) { window.sessionStorage.setItem("activeOrganizationId", active.id); window.sessionStorage.setItem("selectedOrganizationIds", JSON.stringify(ids)); setCompany(active); setCompanies(list); setSelectedIds(ids); setLoading(false); } } void load(); return () => { alive = false; }; }, [router]);
  function selectCompany(id: string) { window.sessionStorage.setItem("activeOrganizationId", id); window.sessionStorage.setItem("selectedOrganizationIds", JSON.stringify([id])); window.location.reload(); }
  if (loading || !company) return <div className="flex min-h-screen items-center justify-center text-sm text-slate-500">جارٍ تحميل مساحة العمل…</div>;
  return <div dir="rtl"><header className="sticky top-0 z-20 h-16 border-b border-slate-200 bg-white"><div className="flex h-full items-center justify-between px-5 sm:px-8"><div className="relative"><button type="button" onClick={() => setOpen((v) => !v)} className="flex items-center gap-3 px-2 py-1.5 text-right hover:bg-slate-50"><span className="flex h-9 w-9 items-center justify-center bg-slate-950 text-sm font-bold text-white">{company.name.slice(0, 1)}</span><span><strong className="block max-w-[20rem] truncate text-sm text-slate-950">{company.name}</strong><small className="text-[11px] text-slate-400">الشركات ▾</small></span></button>{open && <div className="absolute right-0 top-12 z-50 w-80 border border-slate-200 bg-white p-3 shadow-xl"><div className="mb-2 flex items-center justify-between border-b border-slate-100 pb-3"><span className="text-sm font-bold">الشركات</span><button type="button" onClick={() => router.push("/start/new")} className="text-xs font-bold">+ شركة</button></div>{companies.map((item) => <button key={item.id} type="button" onClick={() => selectCompany(item.id)} className="flex w-full items-center justify-between border-b border-slate-100 py-2 text-right last:border-0"><span><span className="block text-sm font-semibold">{item.name}</span><span className="text-[11px] text-slate-400">{roles[item.role_key] ?? item.role_key}</span></span><span className="text-xs">{selectedIds.includes(item.id) ? "✓" : ""}</span></button>)}</div>}</div><div className="flex items-center gap-2"><span className="hidden border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-600 sm:inline-flex">{company.role_key === "company_admin" ? "مدير النظام" : roles[company.role_key] ?? company.role_key}</span><Link href="/start" className="border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-600 hover:bg-slate-50">شركاتي</Link></div></div></header><section className="p-6 sm:p-8 lg:p-10"><div className="border-b border-slate-200 pb-7"><p className="text-xs font-bold tracking-[.16em] text-slate-400">WORKSPACE</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">مساحة عمل {company.name}</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-500 sm:text-base">القائمة الجانبية ثابتة على مستوى مساحة العمل بالكامل وتظهر في جميع الشاشات. الصلاحية تحدد إمكانية الاستخدام فقط؛ الشاشة غير المصرح بها تظل ظاهرة ومقفلة.</p></div><div className="mt-7 grid gap-4 sm:grid-cols-3"><Stat title="الشركة الحالية" value={company.name}/><Stat title="الشركات المحددة" value={`${selectedIds.length}`}/><Stat title="الدور" value={company.role_key === "company_admin" ? "مدير النظام" : roles[company.role_key] ?? company.role_key}/></div></section></div>;
}
function Stat({ title, value }: { title: string; value: string }) { return <div className="border border-slate-200 bg-white p-5"><p className="text-xs font-semibold text-slate-400">{title}</p><p className="mt-2 truncate text-lg font-bold text-slate-950">{value}</p></div>; }
