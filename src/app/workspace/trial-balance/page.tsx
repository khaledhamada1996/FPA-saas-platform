"use client";

import { useEffect, useMemo, useState } from "react";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";

type Period={id:string;period_start:string;period_end:string;status:string};
type TrialRow={code:string;name:string;account_type:string|null;statement_type:string|null;statement_section:string|null;statement_subclassification:string|null;opening_debit:number;opening_credit:number;period_debit:number;period_credit:number;closing_debit:number;closing_credit:number};
type TrialBalance={period_start:string;period_end:string;period_status:string;fiscal_year_start:string;row_count:number;total_period_debit:number;total_period_credit:number;period_difference:number;total_opening_debit:number;total_opening_credit:number;opening_difference:number;total_closing_debit:number;total_closing_credit:number;closing_difference:number;rows:TrialRow[]};
const moneyMinor=(v:number)=>(Number(v||0)/100).toLocaleString("ar-SA",{minimumFractionDigits:2,maximumFractionDigits:2});
const dateLabel=(v:string)=>new Date(`${v}T00:00:00`).toLocaleDateString("ar-SA",{year:"numeric",month:"long",day:"numeric"});
export default function TrialBalancePage(){const [orgId,setOrgId]=useState("");const [periods,setPeriods]=useState<Period[]>([]);const [periodId,setPeriodId]=useState("");const [data,setData]=useState<TrialBalance|null>(null);const [loading,setLoading]=useState(true);const [running,setRunning]=useState(false);const [error,setError]=useState("");useEffect(()=>{const id=window.sessionStorage.getItem("activeOrganizationId")||window.localStorage.getItem("activeOrganizationId")||"";setOrgId(id);if(!id){setLoading(false);setError("لم يتم تحديد مساحة عمل.");return;}void getSupabaseBrowserClient().from("financial_periods").select("id,period_start,period_end,status").eq("organization_id",id).order("period_start",{ascending:false}).then(({data:rows,error:e})=>{if(e)setError(e.message);const next=(rows||[]) as Period[];setPeriods(next);if(next[0])setPeriodId(next[0].id);setLoading(false);});},[]);const selected=useMemo(()=>periods.find(p=>p.id===periodId),[periods,periodId]);async function loadTrialBalance(){if(!orgId||!periodId)return;setRunning(true);setError("");const {data:result,error:e}=await getSupabaseBrowserClient().rpc("get_trial_balance",{p_organization_id:orgId,p_period_id:periodId});if(e){setError(e.message);setData(null);}else setData(result as TrialBalance);setRunning(false);}return <main className="min-h-screen bg-[#f7f8fa] text-[#172033]" dir="rtl"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex w-full max-w-[1500px] items-center justify-between gap-4 px-4 py-4 sm:px-6 lg:px-8"><a href="/workspace" className="text-xs font-semibold text-slate-500 hover:text-slate-950 sm:text-sm">العودة لمساحة العمل</a><div className="text-right"><p className="text-[10px] font-bold tracking-[0.14em] text-slate-400 sm:text-xs">FINANCIAL MODEL</p><h1 className="mt-1 text-base font-bold text-slate-950 sm:text-lg">ميزان المراجعة</h1></div></div></header><section className="mx-auto w-full max-w-[1500px] px-4 py-7 sm:px-6 sm:py-9 lg:px-8"><div className="border-b border-slate-200 pb-7"><p className="text-xs font-bold text-slate-400 sm:text-sm">محرك Trial Balance</p><h2 className="mt-2 text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">ميزان المراجعة الفعلي</h2><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-500 sm:text-base">المصدر الوحيد هو القيود الفعلية المنشورة. الأرصدة الافتتاحية تراعي السنة المالية، والرصيد الختامي يساوي الافتتاحي زائد حركة الفترة.</p></div>{loading?<div className="mt-7 rounded-2xl border border-slate-200 bg-white p-8 text-sm font-semibold text-slate-500">جاري تحميل الفترات المالية...</div>:<><div className="mt-7 grid gap-4 lg:grid-cols-[1fr_auto] lg:items-end"><div className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><label className="text-sm font-bold text-slate-900">الفترة المالية</label><select value={periodId} onChange={e=>{setPeriodId(e.target.value);setData(null)}} className="mt-3 min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm font-semibold text-slate-900">{periods.length===0?<option value="">لا توجد فترات مالية بعد</option>:periods.map(p=><option key={p.id} value={p.id}>{dateLabel(p.period_start)} — {dateLabel(p.period_end)} · {p.status}</option>)}</select></div><button disabled={!periodId||running} onClick={loadTrialBalance} className="min-h-11 rounded-xl bg-slate-950 px-6 py-3 text-sm font-bold text-white disabled:cursor-not-allowed disabled:opacity-50">{running?"جاري الحساب...":"عرض ميزان المراجعة"}</button></div>{selected&&<div className="mt-4 text-xs text-slate-500">الفترة المحددة: {dateLabel(selected.period_start)} إلى {dateLabel(selected.period_end)}</div>}{error&&<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm font-semibold leading-6 text-red-700">{error}</div>}{periods.length===0&&!error&&<div className="mt-7 rounded-2xl border border-amber-200 bg-amber-50 p-6 text-sm font-semibold leading-7 text-amber-800">لا توجد فترة مالية حتى الآن.</div>}{data&&<><div className="mt-7 grid gap-4 sm:grid-cols-3 lg:grid-cols-6"><Metric label="مدين الفترة" value={data.total_period_debit}/><Metric label="دائن الفترة" value={data.total_period_credit}/><Metric label="فرق الفترة" value={data.period_difference} good={data.period_difference===0}/><Metric label="فرق الافتتاح" value={data.opening_difference} good={data.opening_difference===0}/><Metric label="فرق الإغلاق" value={data.closing_difference} good={data.closing_difference===0}/><Metric label="عدد الحسابات" value={data.row_count} raw/></div><div className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[1180px] text-right text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs text-slate-500"><tr><th className="px-4 py-3">كود الحساب</th><th className="px-4 py-3">الحساب</th><th className="px-4 py-3">التصنيف</th><th className="px-4 py-3">مدين الفترة</th><th className="px-4 py-3">دائن الفترة</th><th className="px-4 py-3">افتتاحي مدين</th><th className="px-4 py-3">افتتاحي دائن</th><th className="px-4 py-3">ختامي مدين</th><th className="px-4 py-3">ختامي دائن</th></tr></thead><tbody>{data.rows.map(r=><tr key={r.code} className="border-b border-slate-100 last:border-0"><td className="px-4 py-3 font-mono text-xs font-bold text-slate-700">{r.code}</td><td className="px-4 py-3 font-semibold text-slate-900">{r.name}</td><td className="px-4 py-3 text-xs text-slate-500">{r.statement_subclassification||r.account_type||"غير مصنف"}</td><td className="px-4 py-3 tabular-nums">{moneyMinor(r.period_debit)}</td><td className="px-4 py-3 tabular-nums">{moneyMinor(r.period_credit)}</td><td className="px-4 py-3 tabular-nums">{moneyMinor(r.opening_debit)}</td><td className="px-4 py-3 tabular-nums">{moneyMinor(r.opening_credit)}</td><td className="px-4 py-3 tabular-nums">{moneyMinor(r.closing_debit)}</td><td className="px-4 py-3 tabular-nums">{moneyMinor(r.closing_credit)}</td></tr>)}</tbody></table></div><div className="border-t border-slate-200 px-4 py-4 text-xs font-semibold text-slate-500">السنة المالية تبدأ في {dateLabel(data.fiscal_year_start)} · عدد الحسابات: {data.row_count}</div></div></>}</>}</section></main>}
function Metric({label,value,good=true,raw=false}:{label:string;value:number;good?:boolean;raw?:boolean}){return <div className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-[11px] font-bold text-slate-400">{label}</p><p className={`mt-2 text-lg font-bold ${good?"text-slate-950":"text-red-700"}`}>{raw?Math.round(Number(value||0)):moneyMinor(value)}</p></div>}
