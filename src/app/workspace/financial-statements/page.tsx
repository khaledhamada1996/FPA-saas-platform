"use client";

import { useEffect, useMemo, useState } from "react";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";
import { formatMoneyMinor, formatDate } from "@/lib/format";
import { useDynamicReportFilterOptions, emptyDynamicReportFilters, type DynamicReportFilterState, type DynamicReportFilterOption } from "@/hooks/use-dynamic-report-filter-options";

type StatementKey = "balance" | "income" | "oci" | "cash" | "equity";
type CompareMode = "none" | "previous" | "2_previous" | "3_previous";
type ReportState = { start: string; end: string; compare: CompareMode; cashMethod: "direct" | "indirect" };
const supabase = getSupabaseBrowserClient();
const names: Record<StatementKey, string> = { balance: "قائمة المركز المالي", income: "قائمة الدخل", oci: "الدخل الشامل الآخر", cash: "التدفقات النقدية", equity: "التغيرات في حقوق الملكية" };
const emptyFilters = emptyDynamicReportFilters;
const pad = (n: number) => String(n).padStart(2, "0");
const localIso = (d: Date) => `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
const fromIso = (v: string) => { const [y, m, d] = v.split("-").map(Number); return new Date(y, m - 1, d); };
const addDays = (d: Date, n: number) => { const r = new Date(d.getFullYear(), d.getMonth(), d.getDate()); r.setDate(r.getDate() + n); return r; };
const emptyState: ReportState = { start: "", end: "", compare: "none", cashMethod: "indirect" };

function DateMenu({ state, setState }: { state: ReportState; setState: (v: ReportState) => void }) { const [open, setOpen] = useState(false); const today = new Date(); const anchor = state.end ? fromIso(state.end) : today; const apply = (s: Date, e: Date) => { setState({ ...state, start: localIso(s), end: localIso(e) }); setOpen(false); }; const monthStart = new Date(anchor.getFullYear(), anchor.getMonth(), 1); const monthEnd = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 0); const prevMonth = new Date(anchor.getFullYear(), anchor.getMonth() - 1, 1); const quarterStart = new Date(anchor.getFullYear(), Math.floor(anchor.getMonth() / 3) * 3, 1); const quarterEnd = new Date(anchor.getFullYear(), Math.floor(anchor.getMonth() / 3) * 3 + 3, 0); const yearStart = new Date(anchor.getFullYear(), 0, 1); const yearEnd = new Date(anchor.getFullYear(), 11, 31); const items: [string, () => void][] = [["اليوم", () => apply(today, today)], ["أمس", () => { const d = addDays(today, -1); apply(d, d); }], ["هذا الشهر", () => apply(monthStart, monthEnd)], ["الشهر السابق", () => apply(new Date(prevMonth.getFullYear(), prevMonth.getMonth(), 1), new Date(prevMonth.getFullYear(), prevMonth.getMonth() + 1, 0))], ["هذا الربع", () => apply(quarterStart, quarterEnd)], ["هذه السنة", () => apply(yearStart, yearEnd)], ["كل البيانات", () => { setState({ ...state, start: "", end: "" }); setOpen(false); }]]; return <div className="relative"><button type="button" onClick={() => setOpen(v => !v)} className="min-h-9 border border-slate-300 bg-white px-3 text-xs font-bold">الفترة: {state.start || "كل البيانات"}{state.end ? ` — ${state.end}` : ""}⌄</button>{open && <div className="absolute right-0 top-10 z-50 w-64 border border-slate-300 bg-white shadow-xl">{items.map(([label, action]) => <button key={label} type="button" onClick={action} className="block w-full border-b px-4 py-3 text-right text-xs last:border-0">{label}</button>)}<div className="border-t bg-slate-50 p-3"><input type="date" value={state.start} onChange={e => setState({ ...state, start: e.target.value })} className="mb-2 min-h-9 w-full border bg-white px-2 text-xs"/><input type="date" value={state.end} onChange={e => setState({ ...state, end: e.target.value })} className="min-h-9 w-full border bg-white px-2 text-xs"/></div></div>}</div>; }
function CompareMenu({ state, setState }: { state: ReportState; setState: (v: ReportState) => void }) { const [open, setOpen] = useState(false); const label = state.compare === "none" ? "مقارنة" : state.compare === "previous" ? "الفترة السابقة" : state.compare === "2_previous" ? "فترتان سابقتان" : "3 فترات سابقة"; return <div className="relative"><button type="button" onClick={() => setOpen(v => !v)} className="min-h-9 border border-slate-300 bg-white px-3 text-xs font-bold">{label}⌄</button>{open && <div className="absolute right-0 top-10 z-50 w-56 border border-slate-300 bg-white shadow-xl">{([["none", "بلا مقارنة"], ["previous", "الفترة السابقة"], ["2_previous", "فترتان سابقتان"], ["3_previous", "3 فترات سابقة"]] as const).map(([value, text]) => <button key={value} type="button" onClick={() => { setState({ ...state, compare: value }); setOpen(false); }} className="block w-full border-b px-4 py-3 text-right text-xs">{text}</button>)}</div>}</div>; }
function FilterMenu({ options, filters, setFilters, loading }: { options: Record<string, DynamicReportFilterOption[]>; filters: DynamicReportFilterState; setFilters: (v: DynamicReportFilterState) => void; loading: boolean }) { const [open, setOpen] = useState(false); const fields: [keyof DynamicReportFilterState, string, DynamicReportFilterOption[]][] = [["branch", "الفرع", options.branches || []], ["department", "القسم", options.departments || []], ["costCenter", "مركز التكلفة", options.cost_centers || []], ["region", "المنطقة", options.regions || []], ["product", "المنتج", options.products || []], ["project", "المشروع", options.projects || []], ["account", "الحساب", options.accounts || []]]; const count = Object.values(filters).filter(Boolean).length; return <div className="relative"><button type="button" onClick={() => setOpen(v => !v)} className={`min-h-9 border px-3 text-xs font-bold ${count ? "border-slate-950 bg-slate-950 text-white" : "border-slate-300 bg-white"}`}>الأبعاد{count ? ` · ${count}` : ""}⌄</button>{open && <div className="absolute right-0 top-10 z-50 w-[min(94vw,520px)] border border-slate-300 bg-white p-4 shadow-xl"><div className="mb-3 flex items-center justify-between"><span className="text-xs font-bold">أبعاد التقرير</span><span className="text-[10px] text-slate-400">{loading ? "جاري تحديث الخيارات..." : "مرتبطة بالبيانات"}</span><button type="button" onClick={() => setFilters(emptyFilters)} className="text-[11px] font-bold text-slate-500">مسح الكل</button></div><div className="grid gap-3 sm:grid-cols-2">{fields.map(([key, label, items]) => <label key={key} className="text-[11px] font-bold text-slate-500">{label}<select value={filters[key]} onChange={e => setFilters({ ...filters, [key]: e.target.value })} className="mt-1 min-h-9 w-full border border-slate-300 bg-white px-2 text-xs"><option value="">الكل</option>{items.map(x => <option key={x.id} value={x.id}>{x.code ? `${x.code} — ` : ""}{x.name}</option>)}</select></label>)}</div><p className="mt-3 text-[10px] leading-5 text-slate-400">كل اختيار يعيد حساب الخيارات المتاحة للأبعاد الأخرى تلقائيًا، مع إبقاء الاختيار الحالي صالحًا.</p></div>}</div>; }
function Row({ label, value, compare, level = 0, total = false, muted = false }: { label: string; value: any; compare?: any; level?: number; total?: boolean; muted?: boolean }) { return <div className={`grid grid-cols-[1fr_180px_180px] items-center border-b border-slate-100 py-3 text-sm ${total ? "font-bold" : ""} ${muted ? "text-slate-500" : ""}`}><span style={{ paddingRight: `${level * 22}px` }}>{label}</span><span className="text-left tabular-nums">{formatMoneyMinor(value)}</span><span className="text-left tabular-nums text-slate-400">{compare === undefined ? "—" : formatMoneyMinor(compare)}</span></div>; }
function Box({ title, children }: { title: string; children: React.ReactNode }) { return <section className="border border-slate-200 bg-white p-4"><h3 className="mb-2 text-sm font-bold">{title}</h3>{children}</section>; }
type AccountRow = { id: string; code: string; name: string; balance: number; parent_account_id?: string | null; account_type?: string; statement_section?: string; statement_subclassification?: string };
function HierarchicalAccounts({ accounts, comparison }: { accounts: AccountRow[]; comparison: AccountRow[] }) { const byParent = new Map<string | null, AccountRow[]>(); for (const account of accounts) { const key = account.parent_account_id ?? null; const list = byParent.get(key) || []; list.push(account); byParent.set(key, list); } const compareByCode = new Map((comparison || []).map(x => [x.code, x])); const render = (parentId: string | null, level: number): React.ReactNode => { const children = (byParent.get(parentId) || []).slice().sort((a, b) => a.code.localeCompare(b.code, undefined, { numeric: true })); return children.map(account => { const hasChildren = (byParent.get(account.id) || []).length > 0; const cmp = compareByCode.get(account.code); return <React.Fragment key={account.code}><Row label={`${account.code} — ${account.name}`} value={account.balance} compare={cmp?.balance} level={level} total={hasChildren || level === 0} muted={!hasChildren && level > 0}/>{hasChildren ? render(account.id, level + 1) : null}</React.Fragment>; }); }; return <>{render(null, 0)}</>; }
function Report({ kind, data, comparison }: { kind: StatementKey; data: any; comparison: any }) { if (kind === "balance") return <div className="p-4"><Box title="قائمة المركز المالي"><HierarchicalAccounts accounts={(data?.accounts || []) as AccountRow[]} comparison={(comparison?.accounts || []) as AccountRow[]}/></Box></div>; if (kind === "income") return <div className="max-w-5xl p-4"><Box title="قائمة الدخل"><Row label="الإيرادات" value={data?.revenue} compare={comparison?.revenue} total/><Row label="تكلفة المبيعات" value={data?.cogs} compare={comparison?.cogs}/><Row label="مجمل الربح" value={Number(data?.revenue || 0) - Number(data?.cogs || 0)} compare={Number(comparison?.revenue || 0) - Number(comparison?.cogs || 0)} total/><Row label="المصروفات التشغيلية" value={data?.operating_expenses} compare={comparison?.operating_expenses}/><Row label="الدخل الآخر" value={data?.other_income} compare={comparison?.other_income}/><Row label="صافي الربح أو الخسارة" value={data?.net_income} compare={comparison?.net_income} total/></Box></div>; if (kind === "cash") return <div className="grid gap-4 p-4 md:grid-cols-3"><Box title="التشغيلية"><Row label="صافي التدفق التشغيلي" value={data?.operating?.net_operating_cash_flow} compare={comparison?.operating?.net_operating_cash_flow}/></Box><Box title="الاستثمارية"><Row label="صافي التدفق الاستثماري" value={data?.investing?.net_cash_flow} compare={comparison?.investing?.net_cash_flow}/></Box><Box title="التمويلية"><Row label="صافي التدفق التمويلي" value={data?.financing?.net_cash_flow} compare={comparison?.financing?.net_cash_flow}/></Box><div className="md:col-span-3"><Box title="النقدية"><Row label="النقد أول الفترة" value={data?.opening_cash} compare={comparison?.opening_cash}/><Row label="صافي التغير" value={data?.net_change} compare={comparison?.net_change}/><Row label="النقد آخر الفترة" value={data?.closing_cash} compare={comparison?.closing_cash} total/></Box></div></div>; if (kind === "oci") return <div className="p-4"><Box title="الدخل الشامل الآخر"><Row label="الإجمالي" value={data?.period?.total} compare={comparison?.period?.total} total/>{(data?.period?.rows || []).map((r: any) => <Row key={r.code || r.name} label={`${r.code || ""} — ${r.name || ""}`} value={r.amount}/>)}</Box></div>; return <div className="space-y-4 p-4"><Box title="التغيرات في حقوق الملكية">{(data?.rows || []).map((r: any) => <Row key={`${r.code}-${r.name}`} label={`${r.code} — ${r.name}`} value={r.closing} compare={(comparison?.rows || []).find((x: any) => x.code === r.code)?.closing}/>)}</Box><Box title="الإجماليات"><Row label="صافي الربح YTD" value={data?.ytd_net_income} compare={comparison?.ytd_net_income}/><Row label="حقوق الملكية الختامية" value={data?.displayed_closing_equity} compare={comparison?.displayed_closing_equity} total/></Box></div>; }
export default function FinancialStatementsPage() { const [org, setOrg] = useState(""); const [active, setActive] = useState<StatementKey>("balance"); const [states, setStates] = useState<Record<StatementKey, ReportState>>({ balance: { ...emptyState }, income: { ...emptyState }, oci: { ...emptyState }, cash: { ...emptyState }, equity: { ...emptyState } }); const [filters, setFilters] = useState<DynamicReportFilterState>(emptyFilters); const [data, setData] = useState<any>(null); const [comparison, setComparison] = useState<any>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const state = states[active]; const setState = (value: ReportState) => setStates(s => ({ ...s, [active]: value })); useEffect(() => { const id = window.sessionStorage.getItem("activeOrganizationId") || window.localStorage.getItem("activeOrganizationId") || ""; setOrg(id); if (!id) { setLoading(false); return; } void supabase.from("financial_periods").select("period_start,period_end").eq("organization_id", id).order("period_end", { ascending: false }).limit(1).then(({ data: p }) => { const last = p?.[0]; if (last) setStates(v => Object.fromEntries((Object.keys(names) as StatementKey[]).map(k => [k, { ...v[k], start: last.period_start, end: last.period_end }])) as Record<StatementKey, ReportState>); }); }, []); const { options, loading: optionsLoading, error: optionsError } = useDynamicReportFilterOptions(org, filters, { start: state.start, end: state.end }); useEffect(() => { if (optionsError) setError(optionsError); }, [optionsError]); const comparisonRange = useMemo(() => { if (state.compare === "none" || !state.start || !state.end) return null; const s = fromIso(state.start); const e = fromIso(state.end); const days = Math.max(1, Math.round((e.getTime() - s.getTime()) / 86400000) + 1); const n = state.compare === "previous" ? 1 : state.compare === "2_previous" ? 2 : 3; return { start: localIso(addDays(s, -days * n)), end: localIso(addDays(s, -days * (n - 1) - 1)) }; }, [state.compare, state.start, state.end]); useEffect(() => { if (!org || !state.start || !state.end) return; let cancelled = false; void (async () => { setLoading(true); setError(""); const args = { p_organization_id: org, p_start_date: state.start, p_end_date: state.end, p_journal_no: null, p_cash_flow_method: state.cashMethod, p_branch_id: filters.branch || null, p_department_id: filters.department || null, p_cost_center_id: filters.costCenter || null, p_region_id: filters.region || null, p_product_id: filters.product || null, p_project_id: filters.project || null, p_account_id: filters.account || null }; const a = await supabase.rpc("get_financial_statements_date_range_filtered", args); if (cancelled) return; if (a.error) { setError(a.error.message); setLoading(false); return; } setData(a.data); if (comparisonRange) { const b = await supabase.rpc("get_financial_statements_date_range_filtered", { ...args, p_start_date: comparisonRange.start, p_end_date: comparisonRange.end }); if (!cancelled) { if (b.error) setError(b.error.message); setComparison(b.data || null); } } else setComparison(null); setLoading(false); })(); return () => { cancelled = true; }; }, [org, state.start, state.end, state.cashMethod, comparisonRange, filters.branch, filters.department, filters.costCenter, filters.region, filters.product, filters.project, filters.account]); const key = active === "balance" ? "balance_sheet" : active === "income" ? "income_statement" : active === "cash" ? "cash_flow" : active === "equity" ? "equity_statement" : "other_comprehensive_income"; return <main dir="rtl" className="min-h-screen bg-[#f7f8fa] text-slate-900"><div className="mx-auto w-full px-4 py-5 sm:px-6"><header className="border-b border-slate-200 pb-4"><p className="text-[10px] font-bold tracking-[.16em] text-slate-400">FINANCIAL REPORTING</p><h1 className="mt-1 text-2xl font-bold text-slate-950">القوائم المالية</h1></header><div className="mt-3 flex flex-wrap items-center gap-1 border-b border-slate-200 bg-white px-2 py-2"><DateMenu state={state} setState={setState}/><CompareMenu state={state} setState={setState}/><FilterMenu options={options} filters={filters} setFilters={setFilters} loading={optionsLoading}/>{active === "cash" && <select value={state.cashMethod} onChange={e => setState({ ...state, cashMethod: e.target.value as "direct" | "indirect" })} className="min-h-9 border border-slate-300 bg-white px-3 text-xs"><option value="indirect">التدفقات — غير مباشر</option><option value="direct">التدفقات — مباشر</option></select>}</div><div className="mt-2 flex flex-wrap gap-2 text-[11px] text-slate-500"><span className="border border-slate-200 bg-white px-3 py-1.5">من {state.start || "كل البيانات"} إلى {state.end || "كل البيانات"}</span>{comparisonRange && <span className="border border-slate-200 bg-white px-3 py-1.5">مقارنة: {comparisonRange.start} إلى {comparisonRange.end}</span>}</div><div className="mt-3 overflow-x-auto border border-slate-200 bg-white"><div className="flex min-w-max">{(Object.keys(names) as StatementKey[]).map(k => <button type="button" key={k} onClick={() => setActive(k)} className={`border-l border-slate-200 px-4 py-3 text-xs font-bold ${active === k ? "border-b-2 border-b-slate-950 text-slate-950" : "text-slate-400"}`}>{names[k]}</button>)}</div>{error && <div className="m-3 border border-red-200 bg-red-50 p-3 text-xs text-red-700">{error}</div>}{loading ? <div className="flex min-h-72 items-center justify-center text-sm text-slate-500">جارٍ تحميل القائمة المالية…</div> : <Report kind={active} data={data?.[key]} comparison={comparison?.[key]}/>}</div></div></main>; }