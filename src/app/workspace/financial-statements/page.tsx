"use client";
import { useEffect,useMemo,useState } from "react";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";

type StatementKey="balance"|"income"|"oci"|"cash"|"equity";
type CompareMode="none"|"previous"|"2_previous"|"3_previous";
type Period={id:string;period_start:string;period_end:string;status:string};
type Journal={id:string;name:string};
type ReportState={start:string;end:string;compare:CompareMode;journal:string;cashMethod:"direct"|"indirect"};
const supabase=getSupabaseBrowserClient();
const names:Record<StatementKey,string>={balance:"قائمة المركز المالي",income:"قائمة الدخل",oci:"الدخل الشامل الآخر",cash:"التدفقات النقدية",equity:"التغيرات في حقوق الملكية"};
const money=(v:any)=>(Number(v||0)/100).toLocaleString("ar-SA",{minimumFractionDigits:2,maximumFractionDigits:2});
const dateText=(v:string)=>v?new Date(`${v}T00:00:00`).toLocaleDateString("ar-SA",{year:"numeric",month:"long",day:"numeric"}):"—";
const iso=(d:Date)=>d.toISOString().slice(0,10);
function Row({label,value,compare}:{label:string;value:any;compare?:any}){return <div className="grid grid-cols-[1fr_180px_180px] items-center border-b border-slate-100 py-3 text-sm"><span>{label}</span><span className="text-left tabular-nums">{money(value)}</span><span className="text-left tabular-nums text-slate-400">{compare===undefined?"—":money(compare)}</span></div>}
function Box({title,children}:{title:string;children:React.ReactNode}){return <section className="border border-slate-200 bg-white p-4"><h3 className="mb-2 text-sm font-bold">{title}</h3>{children}</section>}
function DateMenu({state,setState}:{state:ReportState;setState:(v:ReportState)=>void}){const [open,setOpen]=useState(false);const apply=(s:string,e:string)=>{setState({...state,start:s,end:e});setOpen(false)};const end=state.end?new Date(`${state.end}T00:00:00`):new Date();const monthStart=iso(new Date(end.getFullYear(),end.getMonth(),1));const monthEnd=iso(new Date(end.getFullYear(),end.getMonth()+1,0));const prevMonthStart=iso(new Date(end.getFullYear(),end.getMonth()-1,1));const prevMonthEnd=iso(new Date(end.getFullYear(),end.getMonth(),0));const quarter=Math.floor(end.getMonth()/3);const qStart=iso(new Date(end.getFullYear(),quarter*3,1));const qEnd=iso(new Date(end.getFullYear(),quarter*3+3,0));const prevQStart=iso(new Date(end.getFullYear(),quarter*3-3,1));const prevQEnd=iso(new Date(end.getFullYear(),quarter*3,0));const yearStart=iso(new Date(end.getFullYear(),0,1));const yearEnd=iso(new Date(end.getFullYear(),11,31));const prevYearStart=iso(new Date(end.getFullYear()-1,0,1));const prevYearEnd=iso(new Date(end.getFullYear()-1,11,31));return <div className="relative"><button type="button" onClick={()=>setOpen(!open)} className="min-h-9 border border-slate-300 bg-white px-3 text-xs font-bold">اعتبارًا من {dateText(state.end)}⌄</button>{open&&<div className="absolute right-0 top-10 z-50 w-80 border border-slate-300 bg-white shadow-xl"><button type="button" onClick={()=>apply(state.end,state.end)} className="block w-full border-b px-4 py-3 text-right text-xs">اليوم</button><button type="button" onClick={()=>apply(monthStart,monthEnd)} className="block w-full border-b px-4 py-3 text-right text-xs">هذا الشهر</button><button type="button" onClick={()=>apply(prevMonthStart,prevMonthEnd)} className="block w-full border-b px-4 py-3 text-right text-xs">الشهر السابق</button><button type="button" onClick={()=>apply(qStart,qEnd)} className="block w-full border-b px-4 py-3 text-right text-xs">هذا الربع</button><button type="button" onClick={()=>apply(prevQStart,prevQEnd)} className="block w-full border-b px-4 py-3 text-right text-xs">الربع السابق</button><button type="button" onClick={()=>apply(yearStart,yearEnd)} className="block w-full border-b px-4 py-3 text-right text-xs">هذه السنة</button><button type="button" onClick={()=>apply(prevYearStart,prevYearEnd)} className="block w-full border-b px-4 py-3 text-right text-xs">السنة السابقة</button><div className="border-t bg-slate-50 p-3"><div className="mb-2 text-[11px] font-bold">تاريخ محدد</div><div className="grid gap-2"><input type="date" value={state.start} onChange={e=>setState({...state,start:e.target.value})} className="min-h-9 border bg-white px-2 text-xs"/><input type="date" value={state.end} onChange={e=>setState({...state,end:e.target.value})} className="min-h-9 border bg-white px-2 text-xs"/><button type="button" onClick={()=>setOpen(false)} className="min-h-9 bg-slate-950 text-xs font-bold text-white">تطبيق</button></div></div></div>}</div>}
function CompareMenu({state,setState}:{state:ReportState;setState:(v:ReportState)=>void}){const [open,setOpen]=useState(false);const label=state.compare==="none"?"مقارنة":state.compare==="previous"?"الفترة السابقة":state.compare==="2_previous"?"فترتان سابقتان":"3 فترات سابقة";return <div className="relative"><button type="button" onClick={()=>setOpen(!open)} className={`min-h-9 border px-3 text-xs font-bold ${state.compare!=="none"?"border-slate-950 bg-slate-950 text-white":"border-slate-300 bg-white"}`}>{label}⌄</button>{open&&<div className="absolute right-0 top-10 z-50 w-64 border border-slate-300 bg-white shadow-xl"><button type="button" onClick={()=>{setState({...state,compare:"none"});setOpen(false)}} className="block w-full border-b px-4 py-3 text-right text-xs">بلا مقارنة</button><button type="button" onClick={()=>{setState({...state,compare:"previous"});setOpen(false)}} className="block w-full border-b px-4 py-3 text-right text-xs">الفترة السابقة</button><button type="button" onClick={()=>{setState({...state,compare:"2_previous"});setOpen(false)}} className="block w-full border-b px-4 py-3 text-right text-xs">فترتان سابقتان</button><button type="button" onClick={()=>{setState({...state,compare:"3_previous"});setOpen(false)}} className="block w-full px-4 py-3 text-right text-xs">3 فترات سابقة</button></div>}</div>}
function JournalMenu({state,setState,journals}:{state:ReportState;setState:(v:ReportState)=>void;journals:Journal[]}){const [open,setOpen]=useState(false);return <div className="relative"><button type="button" onClick={()=>setOpen(!open)} className={`min-h-9 border px-3 text-xs font-bold ${state.journal?"border-slate-950 bg-slate-950 text-white":"border-slate-300 bg-white"}`}>دفتر اليومية {state.journal?`· ${state.journal}`:"· الكل"}⌄</button>{open&&<div className="absolute right-0 top-10 z-50 w-72 border border-slate-300 bg-white shadow-xl"><button type="button" onClick={()=>{setState({...state,journal:""});setOpen(false)}} className="block w-full border-b px-4 py-3 text-right text-xs">كل دفاتر اليومية</button>{journals.map(j=><button type="button" key={j.id} onClick={()=>{setState({...state,journal:j.id});setOpen(false)}} className="block w-full border-b px-4 py-3 text-right text-xs">{j.name}</button>)}</div>}</div>}
function EntriesMenu(){return <button type="button" disabled className="min-h-9 border border-slate-300 bg-white px-3 text-xs font-bold text-slate-700">قيود مرحّلة · أساس الاستحقاق</button>}
function Report({kind,data,comparison}:{kind:StatementKey;data:any;comparison:any}){if(kind==="balance")return <div className="space-y-4 p-4"><div className="grid gap-3 md:grid-cols-3"><Box title="الأصول"><Row label="إجمالي الأصول" value={data?.total_assets} compare={comparison?.total_assets}/></Box><Box title="الالتزامات"><Row label="إجمالي الالتزامات" value={data?.total_liabilities} compare={comparison?.total_liabilities}/></Box><Box title="حقوق الملكية"><Row label="حقوق الملكية" value={data?.total_equity} compare={comparison?.total_equity}/></Box></div><Box title="الحسابات"><div className="overflow-x-auto"><div className="min-w-[720px]"><div className="grid grid-cols-[1fr_180px_180px] border-b bg-slate-50 px-3 py-2 text-xs font-bold"><span>الحساب</span><span className="text-left">الحالي</span><span className="text-left">المقارنة</span></div>{(data?.accounts||[]).map((r:any)=><Row key={`${r.code}-${r.name}`} label={`${r.code} — ${r.name}`} value={r.balance} compare={(comparison?.accounts||[]).find((x:any)=>x.code===r.code)?.balance}/>)}</div></div></Box></div>;
if(kind==="income")return <div className="max-w-5xl p-4"><Box title="قائمة الدخل"><Row label="الإيرادات" value={data?.revenue} compare={comparison?.revenue}/><Row label="تكلفة المبيعات" value={data?.cogs} compare={comparison?.cogs}/><Row label="مجمل الربح" value={Number(data?.revenue||0)-Number(data?.cogs||0)} compare={Number(comparison?.revenue||0)-Number(comparison?.cogs||0)}/><Row label="المصروفات التشغيلية" value={data?.operating_expenses} compare={comparison?.operating_expenses}/><Row label="الدخل الآخر" value={data?.other_income} compare={comparison?.other_income}/><Row label="الربح قبل الضريبة" value={data?.ebt} compare={comparison?.ebt}/><Row label="ضريبة الدخل" value={data?.tax} compare={comparison?.tax}/><Row label="صافي الربح أو الخسارة" value={data?.net_income} compare={comparison?.net_income}/></Box></div>;
if(kind==="cash")return <div className="grid gap-4 p-4 md:grid-cols-3"><Box title="الأنشطة التشغيلية"><Row label="صافي التدفق التشغيلي" value={data?.operating?.net_operating_cash_flow} compare={comparison?.operating?.net_operating_cash_flow}/></Box><Box title="الأنشطة الاستثمارية"><Row label="صافي التدفق الاستثماري" value={data?.investing?.net_cash_flow} compare={comparison?.investing?.net_cash_flow}/></Box><Box title="الأنشطة التمويلية"><Row label="صافي التدفق التمويلي" value={data?.financing?.net_cash_flow} compare={comparison?.financing?.net_cash_flow}/></Box><div className="md:col-span-3"><Box title="النقدية"><Row label="النقد أول الفترة" value={data?.opening_cash} compare={comparison?.opening_cash}/><Row label="صافي التغير" value={data?.net_change} compare={comparison?.net_change}/><Row label="النقد آخر الفترة" value={data?.closing_cash} compare={comparison?.closing_cash}/><Row label="فرق المطابقة" value={data?.reconciliation_difference} compare={comparison?.reconciliation_difference}/></Box></div></div>;
if(kind==="oci")return <div className="p-4"><Box title="الدخل الشامل الآخر"><Row label="الإجمالي" value={data?.period?.total} compare={comparison?.period?.total}/>{(data?.period?.rows||[]).map((r:any)=><Row key={r.code||r.name} label={`${r.code||""} — ${r.name||""}`} value={r.amount}/>)}</Box></div>;
return <div className="space-y-4 p-4"><Box title="التغيرات في حقوق الملكية">{(data?.rows||[]).map((r:any)=><Row key={`${r.code}-${r.name}`} label={`${r.code} — ${r.name}`} value={r.closing} compare={(comparison?.rows||[]).find((x:any)=>x.code===r.code)?.closing}/>)}</Box><Box title="الإجماليات"><Row label="صافي الربح YTD" value={data?.ytd_net_income} compare={comparison?.ytd_net_income}/><Row label="حقوق الملكية الختامية المعروضة" value={data?.displayed_closing_equity} compare={comparison?.displayed_closing_equity}/></Box></div>}
export default function FinancialStatementsPage(){const [org,setOrg]=useState("");const [journals,setJournals]=useState<Journal[]>([]);const [active,setActive]=useState<StatementKey>("balance");const empty:ReportState={start:"",end:"",compare:"none",journal:"",cashMethod:"indirect"};const [states,setStates]=useState<Record<StatementKey,ReportState>>({balance:{...empty},income:{...empty},oci:{...empty},cash:{...empty},equity:{...empty}});const [data,setData]=useState<any>(null);const [comparison,setComparison]=useState<any>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState("");
useEffect(()=>{const id=window.sessionStorage.getItem("activeOrganizationId")||"";setOrg(id);if(!id){setLoading(false);return}void(async()=>{const {data:j}=await supabase.rpc("get_financial_report_journal_options",{p_organization_id:id});setJournals((j||[]) as Journal[]);const {data:p}=await supabase.from("financial_periods").select("period_start,period_end").eq("organization_id",id).order("period_end",{ascending:false}).limit(1);const last=p?.[0];if(last){setStates(v=>Object.fromEntries((Object.keys(names) as StatementKey[]).map(k=>[k,{...v[k],start:last.period_start,end:last.period_end}])) as Record<StatementKey,ReportState>);}})()},[]);
const state=states[active];const setState=(v:ReportState)=>setStates(s=>({...s,[active]:v}));const comparisonRange=useMemo(()=>{if(state.compare==="none"||!state.start||!state.end)return null;const s=new Date(`${state.start}T00:00:00`);const e=new Date(`${state.end}T00:00:00`);const days=Math.max(1,Math.round((e.getTime()-s.getTime())/86400000)+1);const n=state.compare==="previous"?1:state.compare==="2_previous"?2:3;return {start:iso(new Date(s.getTime()-days*n*86400000)),end:iso(new Date(s.getTime()-days*(n-1)*86400000-86400000))}},[state.compare,state.start,state.end]);
useEffect(()=>{if(!org||!state.start||!state.end)return;let cancelled=false;void(async()=>{setLoading(true);setError("");const args={p_organization_id:org,p_start_date:state.start,p_end_date:state.end,p_journal_no:state.journal||null,p_cash_flow_method:state.cashMethod};const a=await supabase.rpc("get_financial_statements_date_range_exact",args);if(cancelled)return;if(a.error){setError(a.error.message);setLoading(false);return}setData(a.data);if(comparisonRange){const b=await supabase.rpc("get_financial_statements_date_range_exact",{...args,p_start_date:comparisonRange.start,p_end_date:comparisonRange.end});if(!cancelled){if(b.error)setError(b.error.message);setComparison(b.data||null)}}else setComparison(null);setLoading(false)})();return()=>{cancelled=true}},[org,state.start,state.end,state.journal,state.cashMethod,comparisonRange]);
return <main dir="rtl" className="min-h-screen bg-[#f7f8fa] text-slate-900"><div className="mx-auto w-full max-w-[1500px] px-4 py-5 sm:px-6"><header className="border-b border-slate-200 pb-4"><p className="text-[10px] font-bold tracking-[.16em] text-slate-400">FINANCIAL REPORTING</p><h1 className="mt-1 text-2xl font-bold text-slate-950">القوائم المالية</h1></header><div className="mt-3 flex flex-wrap items-center gap-1 border-b border-slate-200 bg-white px-2 py-2"><DateMenu state={state} setState={setState}/><CompareMenu state={state} setState={setState}/><JournalMenu state={state} setState={setState} journals={journals}/><EntriesMenu/></div><div className="mt-2 flex flex-wrap gap-2 text-[11px] text-slate-500"><span className="border border-slate-200 bg-white px-3 py-1.5">من {dateText(state.start)} إلى {dateText(state.end)}</span>{comparisonRange&&<span className="border border-slate-200 bg-white px-3 py-1.5">مقارنة: {dateText(comparisonRange.start)} إلى {dateText(comparisonRange.end)}</span>}</div><div className="mt-3 overflow-x-auto border border-slate-200 bg-white"><div className="flex min-w-max">{(Object.keys(names) as StatementKey[]).map(k=><button type="button" key={k} onClick={()=>setActive(k)} className={`border-l border-slate-200 px-4 py-3 text-xs font-bold ${active===k?"border-b-2 border-b-slate-950 text-slate-950":"text-slate-400"}`}>{names[k]}</button>)}</div>{comparisonRange&&<div className="grid grid-cols-[1fr_180px_180px] border-t px-4 py-2 text-[11px] font-bold text-slate-500"><span>البند</span><span className="text-left">الفترة الحالية</span><span className="text-left">الفترة المقارنة</span></div>}{error&&<div className="m-3 border border-red-200 bg-red-50 p-3 text-xs text-red-700">{error}</div>}{loading?<div className="flex min-h-72 items-center justify-center text-sm text-slate-500">جارٍ تحميل القائمة المالية…</div>:<Report kind={active} data={data?.[active==="balance"?"balance_sheet":active==="income"?"income_statement":active==="cash"?"cash_flow":active==="equity"?"equity_statement":"other_comprehensive_income"]} comparison={comparison?.[active==="balance"?"balance_sheet":active==="income"?"income_statement":active==="cash"?"cash_flow":active==="equity"?"equity_statement":"other_comprehensive_income"]}/>}</div></div></main>}
