"use client";
import { useEffect,useState } from "react";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";
type Period={id:string;period_start:string;period_end:string;status:string};
type FS={period:{start:string;end:string;status:string};income_statement:{revenue:number;cogs:number;gross_profit:number;operating_expenses:number;ebitda:number;finance_cost:number;ebt:number;tax:number;net_income:number};balance_sheet:{accounts:Array<{code:string;name:string;section:string|null;account_type:string|null;debit:number;credit:number;balance:number}>;total_assets:number;total_liabilities:number;total_equity:number}};
const money=(v:number)=>(Number(v||0)/100).toLocaleString("ar-SA",{minimumFractionDigits:2,maximumFractionDigits:2});
const date=(v:string)=>new Date(`${v}T00:00:00`).toLocaleDateString("ar-SA",{year:"numeric",month:"long",day:"numeric"});
export default function FinancialStatementsPage(){const [org,setOrg]=useState("");const [periods,setPeriods]=useState<Period[]>([]);const [period,setPeriod]=useState("");const [data,setData]=useState<FS|null>(null);const [error,setError]=useState("");const [loading,setLoading]=useState(true);const [running,setRunning]=useState(false);
useEffect(()=>{const id=window.sessionStorage.getItem("activeOrganizationId")||window.localStorage.getItem("activeOrganizationId")||"";setOrg(id);if(!id){setError("لم يتم تحديد مساحة عمل.");setLoading(false);return;}const s=getSupabaseBrowserClient();void s.from("financial_periods").select("id,period_start,period_end,status").eq("organization_id",id).order("period_start",{ascending:false}).then(({data,error})=>{if(error)setError(error.message);const p=(data||[]) as Period[];setPeriods(p);if(p[0])setPeriod(p[0].id);setLoading(false);});},[]);
async function load(){if(!org||!period)return;setRunning(true);setError("");const {data,error}=await getSupabaseBrowserClient().rpc("get_financial_statements",{p_organization_id:org,p_period_id:period});if(error){setError(error.message);setData(null);}else setData(data as FS);setRunning(false);}
return <main dir="rtl" className="min-h-screen bg-[#f7f8fa] text-[#172033]"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-[1500px] items-center justify-between px-4 py-4 sm:px-6 lg:px-8"><a href="/workspace" className="text-sm font-semibold text-slate-500">العودة لمساحة العمل</a><div><p className="text-[10px] font-bold tracking-[.14em] text-slate-400">FINANCIAL MODEL</p><h1 className="mt-1 font-bold text-slate-950">القوائم المالية</h1></div></div></header><section className="mx-auto max-w-[1500px] px-4 py-7 sm:px-6 lg:px-8"><div className="border-b border-slate-200 pb-7"><p className="text-xs font-bold text-slate-400">FINANCIAL STATEMENTS</p><h2 className="mt-2 text-2xl font-bold text-slate-950 sm:text-3xl">القوائم المالية الفعلية</h2><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-500">قائمة الدخل والميزانية العمومية محسوبتان من البيانات الفعلية المنشورة، دون استخدام الذكاء الاصطناعي كمحرك للحسابات.</p></div>{loading?<div className="mt-7 rounded-2xl border border-slate-200 bg-white p-8 text-sm text-slate-500">جاري تحميل الفترات المالية...</div>:<><div className="mt-7 flex flex-col gap-4 sm:flex-row sm:items-end"><label className="flex-1 rounded-2xl border border-slate-200 bg-white p-5"><span className="text-sm font-bold text-slate-900">الفترة المالية</span><select value={period} onChange={e=>{setPeriod(e.target.value);setData(null)}} className="mt-3 min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm">{periods.map(p=><option key={p.id} value={p.id}>{date(p.period_start)} — {date(p.period_end)}</option>)}</select></label><button disabled={!period||running} onClick={load} className="min-h-11 rounded-xl bg-slate-950 px-6 py-3 text-sm font-bold text-white disabled:opacity-50">{running?"جاري الحساب...":"عرض القوائم"}</button></div>{error&&<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm font-semibold text-red-700">{error}</div>}{data&&<><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["الإيرادات",data.income_statement.revenue],["مجمل الربح",data.income_statement.gross_profit],["EBITDA",data.income_statement.ebitda],["صافي الربح",data.income_statement.net_income]].map(([k,v])=><div key={String(k)} className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-xs font-bold text-slate-400">{k}</p><p className="mt-2 text-xl font-bold text-slate-950">{money(Number(v))}</p></div>)}</div><div className="mt-7 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><h3 className="text-lg font-bold text-slate-950">قائمة الدخل</h3><div className="mt-5 space-y-3">{[["الإيرادات",data.income_statement.revenue],["تكلفة المبيعات",data.income_statement.cogs],["مجمل الربح",data.income_statement.gross_profit],["المصروفات التشغيلية",data.income_statement.operating_expenses],["EBITDA",data.income_statement.ebitda],["تكلفة التمويل",data.income_statement.finance_cost],["الربح قبل الضريبة",data.income_statement.ebt],["الضريبة",data.income_statement.tax],["صافي الربح",data.income_statement.net_income]].map(([k,v],i)=><div key={String(k)} className={`flex justify-between border-b border-slate-100 py-2.5 text-sm ${i===2||i===4||i===8?"font-bold text-slate-950":"text-slate-600"}`}><span>{k}</span><span className="tabular-nums">{money(Number(v))}</span></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><h3 className="text-lg font-bold text-slate-950">الميزانية العمومية</h3><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">إجمالي الأصول</p><p className="mt-1 font-bold">{money(data.balance_sheet.total_assets)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">الالتزامات</p><p className="mt-1 font-bold">{money(data.balance_sheet.total_liabilities)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">حقوق الملكية</p><p className="mt-1 font-bold">{money(data.balance_sheet.total_equity)}</p></div></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[620px] text-right text-sm"><thead className="border-b border-slate-200 text-xs text-slate-500"><tr><th className="px-2 py-3">الحساب</th><th className="px-2 py-3">التصنيف</th><th className="px-2 py-3">الرصيد</th></tr></thead><tbody>{data.balance_sheet.accounts.map(a=><tr key={a.code} className="border-b border-slate-100"><td className="px-2 py-3 font-semibold">{a.code} — {a.name}</td><td className="px-2 py-3 text-slate-500">{a.account_type||a.section||"—"}</td><td className="px-2 py-3 tabular-nums">{money(a.balance)}</td></tr>)}</tbody></table></div></section></div></>}</>}</section></main>}
