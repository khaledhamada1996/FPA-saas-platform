"use client";
import { useEffect,useMemo,useState } from "react";
import { useRouter } from "next/navigation";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";
type Company={id:string;name:string;legal_name:string|null;industry:string|null;city:string|null;base_currency:string;role:string;role_key:string;parent_organization_id:string|null;children_count:number;can_manage_children:boolean};
const roleLabels:Record<string,string>={owner:"مالك",admin:"مسؤول الشركة",company_admin:"مسؤول الشركة",finance_manager:"مدير مالي",cfo:"المدير المالي",accountant:"محاسب",fpa_analyst:"محلل FP&A",viewer:"مطلع"};
export default function StartPage(){const router=useRouter();const [state,setState]=useState<"loading"|"empty"|"companies">("loading"),[companies,setCompanies]=useState<Company[]>([]),[expanded,setExpanded]=useState<Set<string>>(new Set()),[error,setError]=useState(""),[deleteTarget,setDeleteTarget]=useState<Company|null>(null),[confirmation,setConfirmation]=useState(""),[deleting,setDeleting]=useState(false),[deleteError,setDeleteError]=useState("");
useEffect(()=>{let active=true;const s=getSupabaseBrowserClient();async function load(){const {data:userData,error:userError}=await s.auth.getUser();if(userError||!userData.user){router.replace("/login?next=/start");return;}const {data,error:e}=await s.rpc("get_my_workspaces");if(e){if(active){setError("تعذر تحميل الشركات المرتبطة بحسابك.");setState("empty");}return;}const cs=(data??[]) as Company[];if(!active)return;if(cs.length===0){setState("empty");return;}if(cs.length===1){window.sessionStorage.setItem("activeOrganizationId",cs[0].id);router.replace("/workspace");return;}setCompanies(cs);setExpanded(new Set(cs.filter(c=>c.children_count>0).map(c=>c.id)));setState("companies");}void load();return()=>{active=false};},[router]);
function enterCompany(c:Company){window.sessionStorage.setItem("activeOrganizationId",c.id);router.push("/workspace")}function editCompany(c:Company){router.push(`/start/edit?id=${encodeURIComponent(c.id)}`)}function toggleCompany(id:string){setExpanded(cur=>{const n=new Set(cur);n.has(id)?n.delete(id):n.add(id);return n})}function addSubsidiary(c:Company){router.push(`/start/new?parent=${encodeURIComponent(c.id)}`)}function openDelete(c:Company){setDeleteTarget(c);setConfirmation("");setDeleteError("")}function closeDelete(){if(deleting)return;setDeleteTarget(null);setConfirmation("");setDeleteError("")}
async function deleteCompany(){if(!deleteTarget||confirmation!==deleteTarget.name)return;setDeleting(true);setDeleteError("");try{const s=getSupabaseBrowserClient();const {data,error:e}=await s.rpc("delete_workspace",{p_organization_id:deleteTarget.id,p_confirmation_name:confirmation});if(e)throw e;if(!data)throw new Error("تعذر حذف الشركة.");if(window.sessionStorage.getItem("activeOrganizationId")===deleteTarget.id)window.sessionStorage.removeItem("activeOrganizationId");const remaining=companies.filter(c=>c.id!==deleteTarget.id);setDeleteTarget(null);setConfirmation("");if(remaining.length===0){setCompanies([]);setState("empty")}else if(remaining.length===1){window.sessionStorage.setItem("activeOrganizationId",remaining[0].id);router.replace("/workspace")}else setCompanies(remaining)}catch(e){setDeleteError(e instanceof Error?e.message:"تعذر حذف الشركة.")}finally{setDeleting(false)}}
const tree=useMemo(()=>buildCompanyTree(companies),[companies]);if(state==="loading")return <CenteredState text="جارٍ تحميل مساحة العمل…"/>;if(state==="empty")return <main className="min-h-screen bg-[#f7f8fa] px-4 py-8 text-[#172033] sm:px-6" dir="rtl"><div className="mx-auto flex min-h-[calc(100dvh-4rem)] w-full max-w-5xl items-center justify-center"><section className="w-full max-w-2xl rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:p-10"><div className="flex flex-col gap-6 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-bold tracking-[0.16em] text-slate-400">WELCOME TO FP&A</p><h1 className="mt-3 text-2xl font-bold text-slate-950 sm:text-3xl">لننشئ أول شركة لك</h1><p className="mt-3 max-w-xl text-sm leading-7 text-slate-500 sm:text-base">لن نطلب منك معلومات الشركة في كل تسجيل دخول. هذه الشاشة تظهر فقط عندما لا توجد شركة مرتبطة بحسابك.</p></div><span className="shrink-0 rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold text-slate-600">لا توجد شركات</span></div>{error&&<div className="mt-6 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<button type="button" onClick={()=>router.push("/start/new")} className="mt-8 w-full rounded-xl bg-slate-950 px-6 py-4 text-sm font-bold text-white hover:bg-slate-800 sm:w-auto">إضافة شركة</button></section></div></main>;
return <main className="min-h-screen bg-[#f7f8fa] px-4 py-6 text-[#172033] sm:px-6 lg:px-10" dir="rtl"><header className="mx-auto flex w-full max-w-7xl items-center justify-between gap-4 border-b border-slate-200 pb-5"><div className="min-w-0"><p className="text-xs font-bold tracking-[0.16em] text-slate-400">FP&A</p><h1 className="mt-1 truncate text-lg font-bold text-slate-950">شركاتي</h1></div><button type="button" onClick={()=>router.push("/start/new")} className="shrink-0 rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white hover:bg-slate-800 sm:px-5">+ إضافة شركة</button></header><section className="mx-auto w-full max-w-7xl py-8 sm:py-10 lg:py-14"><div className="max-w-3xl"><p className="text-xs font-bold tracking-[0.16em] text-slate-400">YOUR ORGANIZATION GROUP</p><h2 className="mt-3 text-3xl font-bold text-slate-950 sm:text-4xl">هيكل الشركات</h2><p className="mt-3 text-sm leading-7 text-slate-500 sm:text-base">أدير المجموعة من هنا: الشركة الأم، الشركات التابعة، ثم الشركات التشغيلية تحتها. اضغط السهم لفتح أو إغلاق أي فرع.</p></div><div className="mt-8 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="flex flex-col gap-4 border-b border-slate-100 pb-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="text-lg font-bold text-slate-950">شجرة المجموعة</h3><p className="mt-1 text-sm text-slate-500">{companies.length} شركة في مساحة العمل</p></div><div className="flex flex-wrap gap-2 text-xs font-semibold text-slate-500"><span className="rounded-full bg-slate-100 px-3 py-1.5">شركة أم</span><span className="rounded-full bg-blue-50 px-3 py-1.5 text-blue-700">شركة تابعة</span></div></div><div className="mt-7 space-y-3">{tree.map(n=><CompanyTreeNode key={n.company.id} node={n} depth={0} expanded={expanded} onToggle={toggleCompany} onEnter={enterCompany} onEdit={editCompany} onAddSubsidiary={addSubsidiary} onDelete={openDelete}/>)}</div></div><div className="mt-8 rounded-2xl border border-slate-200 bg-white p-5 text-sm leading-7 text-slate-500 sm:p-6"><span className="font-bold text-slate-900">هيكل المجموعة:</span> يمكن بناء مستويات متعددة مثل شركة قابضة ← شركة تابعة ← شركة تشغيلية. إضافة شركة تابعة وتعديل بيانات الشركة يمران بتحقق الصلاحية من قاعدة البيانات، وليس من واجهة المستخدم فقط.</div></section>{deleteTarget&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4" role="dialog" aria-modal="true" aria-labelledby="delete-company-title"><div className="w-full max-w-lg rounded-3xl border border-slate-200 bg-white p-6 shadow-2xl sm:p-8"><p className="text-xs font-bold tracking-[0.14em] text-red-500">DANGER ZONE</p><h2 id="delete-company-title" className="mt-2 text-2xl font-bold text-slate-950">حذف الشركة نهائيًا</h2><p className="mt-3 text-sm leading-7 text-slate-600">هذا الإجراء يحذف مساحة الشركة وبياناتها المرتبطة بها ولا ينبغي تنفيذه إلا إذا كنت متأكدًا تمامًا.</p><div className="mt-5 rounded-2xl border border-red-100 bg-red-50 p-4 text-sm leading-7 text-red-800"><strong>{deleteTarget.name}</strong><br/>للتأكيد، اكتب اسم الشركة كما هو بالضبط في الحقل التالي.</div><label className="mt-5 block"><span className="mb-2 block text-sm font-semibold text-slate-700">اكتب اسم الشركة للتأكيد</span><input className="input" value={confirmation} onChange={e=>setConfirmation(e.target.value)} placeholder={deleteTarget.name} autoFocus autoComplete="off"/></label>{deleteError&&<div className="mt-4 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm leading-6 text-red-700" role="alert">{deleteError}</div>}<div className="mt-6 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button type="button" disabled={deleting} onClick={closeDelete} className="rounded-xl border border-slate-200 px-5 py-3 text-sm font-bold text-slate-700 hover:bg-slate-50">إلغاء</button><button type="button" disabled={deleting||confirmation!==deleteTarget.name} onClick={()=>void deleteCompany()} className="rounded-xl bg-red-600 px-5 py-3 text-sm font-bold text-white hover:bg-red-700 disabled:cursor-not-allowed disabled:opacity-40">{deleting?"جارٍ الحذف…":"حذف الشركة نهائيًا"}</button></div></div></div>}</main>}
type Node={company:Company;children:Node[]};function buildCompanyTree(cs:Company[]):Node[]{const nodes=new Map<string,Node>();for(const c of cs)nodes.set(c.id,{company:c,children:[]});const roots:Node[]=[];for(const c of cs){const n=nodes.get(c.id)!;const p=c.parent_organization_id?nodes.get(c.parent_organization_id):undefined;if(p&&p.company.id!==c.id)p.children.push(n);else roots.push(n)}const sort=(xs:Node[])=>{xs.sort((a,b)=>a.company.name.localeCompare(b.company.name,"ar"));for(const x of xs)sort(x.children)};sort(roots);return roots}
function CompanyTreeNode({node,depth,expanded,onToggle,onEnter,onEdit,onAddSubsidiary,onDelete}:{node:Node;depth:number;expanded:Set<string>;onToggle:(id:string)=>void;onEnter:(c:Company)=>void;onEdit:(c:Company)=>void;onAddSubsidiary:(c:Company)=>void;onDelete:(c:Company)=>void}){const c=node.company,hasChildren=node.children.length>0,isExpanded=expanded.has(c.id),canManage=c.role_key==="company_admin"||c.role==="owner"||c.role==="admin";return <div className={depth>0?"mr-5 border-r-2 border-slate-200 pr-5 sm:mr-8 sm:pr-7":""}><article className="rounded-2xl border border-slate-200 bg-slate-50/60 p-4 transition hover:border-slate-300 hover:bg-white hover:shadow-sm sm:p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex min-w-0 items-center gap-3">{hasChildren?<button type="button" onClick={()=>onToggle(c.id)} aria-label={isExpanded?`طي ${c.name}`:`فتح ${c.name}`} className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl border border-slate-200 bg-white text-slate-600 hover:bg-slate-100">{isExpanded?"−":"+"}</button>:<span className="h-9 w-9 shrink-0"/>}<button type="button" onClick={()=>onEnter(c)} className="flex min-w-0 items-center gap-4 text-right"><div className={`flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl text-lg font-bold text-white ${depth===0?"bg-slate-950":"bg-blue-700"}`}>{c.name.slice(0,1)}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h4 className="truncate text-base font-bold text-slate-950 sm:text-lg">{c.name}</h4><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${depth===0?"bg-slate-200 text-slate-700":"bg-blue-100 text-blue-700"}`}>{depth===0?"شركة أم":"شركة تابعة"}</span></div><p className="mt-1 text-xs text-slate-500">{c.industry||"قطاع غير محدد"}{c.city?` · ${c.city}`:""}</p></div></button></div><div className="flex flex-wrap items-center gap-2 sm:justify-end"><span className="rounded-full bg-white px-3 py-1.5 text-xs font-semibold text-slate-600 ring-1 ring-slate-200">{roleLabels[c.role_key]??roleLabels[c.role]??c.role_key}</span>{hasChildren&&<span className="rounded-full bg-blue-50 px-3 py-1.5 text-xs font-semibold text-blue-700">{node.children.length} تابع</span>}{c.can_manage_children&&<button type="button" onClick={()=>onAddSubsidiary(c)} className="rounded-xl border border-slate-200 bg-white px-3.5 py-2 text-xs font-bold text-slate-700 hover:bg-slate-100">+ شركة تابعة</button>}{canManage&&<button type="button" onClick={()=>onEdit(c)} className="rounded-xl border border-slate-200 bg-white px-3.5 py-2 text-xs font-bold text-slate-700 hover:bg-slate-100">تعديل الشركة</button>}<button type="button" onClick={()=>onEnter(c)} className="rounded-xl bg-slate-950 px-3.5 py-2 text-xs font-bold text-white hover:bg-slate-800">دخول</button>{canManage&&<button type="button" onClick={()=>onDelete(c)} className="rounded-xl border border-red-200 bg-white px-3.5 py-2 text-xs font-bold text-red-600 hover:bg-red-50">حذف</button>}</div></div></article>{hasChildren&&isExpanded&&<div className="mt-3 space-y-3">{node.children.map(ch=><CompanyTreeNode key={ch.company.id} node={ch} depth={depth+1} expanded={expanded} onToggle={onToggle} onEnter={onEnter} onEdit={onEdit} onAddSubsidiary={onAddSubsidiary} onDelete={onDelete}/>)}</div>}</div>}
function CenteredState({text}:{text:string}){return <main className="min-h-screen bg-[#f7f8fa] px-4 py-8 text-[#172033]" dir="rtl"><div className="mx-auto flex min-h-[calc(100dvh-4rem)] w-full max-w-3xl items-center justify-center"><div className="w-full rounded-2xl border border-slate-200 bg-white p-8 text-center shadow-sm"><p className="text-sm font-semibold text-slate-600">{text}</p></div></div></main>}
